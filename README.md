# configuration-management-p1-2025-2
Implement version management in a collaborative development project using Git, working with branches, merging changes, and resolving conflicts in a multi- developer environment. Students can choose between a backend web application or a frontend web application project based on their preference.

Backend:
**Michael Jiménez:** Implemented the `GET /getAll` endpoint to retrieve all tasks from memory.
- **Michael Jiménez:** Implemented the `POST /create` endpoint to add a new task using the field received in the request body.
- **Michael Jiménez:** Implemented the `PUT /complete/:id` endpoint to toggle a task’s completion status, mark as completed or pending.

Frontend: 
- Daniel Picon: Mostrar lista de todas las tareas.
